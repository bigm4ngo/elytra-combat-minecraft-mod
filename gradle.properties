org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true
org.gradle.configuration-cache=false

# Release target override: -Ptarget=<id>, see the TARGETS map in build.gradle.
# Default builds the 26.1.x release jar.
loom_version=1.15.5
loader_version=0.18.4

mod_version=1.3.3
maven_group=dev.elytracombat
archives_base_name=elytra-combat
